# FIXTURE — deliberately invalid. Every entry below violates cv.schema.json in a
# different way, so a negative test can assert the transform reports all of them
# rather than dying on the first.
experience:
  # 1. missing the required `position`
  - organization: University of Porto
    start: "2020-01"
education:
  # 2. `university` must be a list of strings, not a bare string
  - degree: PhD
    university: University of Porto
  # 3. date is not YYYY / YYYY-MM / YYYY-MM-DD
  - degree: MSc
    start: "September 2014"
skills:
  # 4. `level` must be an integer, not a string
  - name: Python
    level: "five"
languages:
  # 5. `mother_tongue` must be a boolean
  - name: Portuguese
    mother_tongue: "yes"
research_interests:
  # 6. `name` must be a non-empty string
  - name: ""
teaching:
  supervised_students:
    # 7. `status` outside the allowed enum
    - name: Ana Example
      status: halfway
  jury:
    # 8. `jury_role` outside the allowed enum
    - name: Carla Example
      jury_role: Chairperson
