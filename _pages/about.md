---
layout: about
title: about
permalink: /
subtitle:

profile:
  align: right
  image_circular: false # crops the image to make it circular
  more_info:

selected_papers: false # Phase 4 turns this on, once _bibliography/papers.bib is real
social: true # includes social icons at the bottom of the page

announcements:
  enabled: false # Phase 6 decides whether _news/ is used at all
  scrollable: true # adds a vertical scroll bar if there are more than 3 news items
  limit: 5 # leave blank to include all the news in the `_news` folder

latest_posts:
  enabled: false # Phase 6 turns this on, once _posts/ is generated from the graph
  scrollable: true # adds a vertical scroll bar if there are more than 3 new posts items
  limit: 3 # leave blank to include all the blog posts
---

This site is being rebuilt. The content pipeline that fills it — CV, publications, blog and
personal pages, all authored in a Logseq graph and transformed into this repository's data
files — is described in [`seed.md`](https://github.com/pdlourenco/pdlourenco.github.io/blob/master/seed.md)
and [`docs/IMPLEMENTATION-PLAN.md`](https://github.com/pdlourenco/pdlourenco.github.io/blob/master/docs/IMPLEMENTATION-PLAN.md).

<!-- TODO(owner): replace this placeholder with the real bio, and add `subtitle:` and
     `profile.image:` (drop a photo at assets/img/ and name it here). Nothing on this page
     is generated yet — the About text becomes graph-sourced when profile.yml lands. -->
